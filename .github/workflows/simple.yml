name: shell_commmands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps: 
      - name: echo string
        run: echo "Hello world"

      - name: echo another string
        run: |
          node -v 
          npm -v
          echo "Hello using multiline"